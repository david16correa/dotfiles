binds {
  // help
  Mod+Shift+Slash { show-hotkey-overlay; }

  // general launchers
  Mod+Return                 hotkey-overlay-title="Open a Terminal"              { spawn "kitty"; }
  Mod+Space                  hotkey-overlay-title="Run an Application: noctalia" { spawn-sh "noctalia-shell ipc call launcher toggle"; }
  Alt+Space                  hotkey-overlay-title="Run an Application: vicinae"  { spawn-sh "vicinae toggle"; }
  Mod+S                      hotkey-overlay-title="Control Center"               { spawn-sh "noctalia-shell ipc call controlCenter toggle"; }
  Mod+Shift+Return           hotkey-overlay-title="Settings"                     { spawn-sh "noctalia-shell ipc call settings toggle"; }
  Mod+M                      hotkey-overlay-title="Calendar"                     { spawn-sh "noctalia-shell ipc call calendar toggle"; }
  Mod+N                      hotkey-overlay-title="Notification History"         { spawn-sh "noctalia-shell ipc call notifications toggleHistory"; }
  Mod+Ctrl+N                 hotkey-overlay-title="Do Not Disturb"               { spawn-sh "noctalia-shell ipc call notifications toggleDND"; }
  // Mod+V                      hotkey-overlay-title="Clipboard History"            { spawn-sh "noctalia-shell ipc call launcher clipboard"; }

  // session actions
  Mod+Shift+Alt+Ctrl+C       hotkey-overlay-title="Caffeinate"                   { spawn-sh "noctalia-shell ipc call idleInhibitor toggle"; }
  Mod+Shift+Alt+Ctrl+Return  hotkey-overlay-title="Session Menu"                 { spawn-sh "noctalia-shell ipc call sessionMenu toggle"; }
  Mod+Shift+Alt+Ctrl+L       hotkey-overlay-title="Lock the Screen"              { spawn-sh "noctalia-shell ipc call lockScreen lock"; }
  Mod+Shift+Alt+Ctrl+Q       hotkey-overlay-title="Lock and Suspend"             { spawn-sh "noctalia-shell ipc call lockScreen lock && systemctl suspend"; }
  Mod+Shift+Alt+Ctrl+H       hotkey-overlay-title="Lock and Hibernate"           { spawn-sh "noctalia-shell ipc call lockScreen lock && systemctl hibernate"; }
  Mod+Shift+Alt+Ctrl+P       hotkey-overlay-title="Power off all montiors"       { power-off-monitors; }
  Ctrl+Alt+Delete            hotkey-overlay-title="Quit Niri"                    { quit; }

  // favourite apps
  // Mod+1                      hotkey-overlay-title="Open (or focus) Files"        { spawn-sh "/home/david/.myScripts/focusOrLaunch nemo nemo"; }
  Mod+1                      hotkey-overlay-title="Open (or focus) Files"        { spawn-sh "/home/david/.myScripts/focusOrLaunch org.gnome.Nautilus nautilus"; }
  Mod+2                      hotkey-overlay-title="Open (or focus) a Terminal"   { spawn-sh "/home/david/.myScripts/focusOrLaunch kitty kitty"; }
  Mod+3                      hotkey-overlay-title="Open (or focus) Zen Browser"  { spawn-sh "/home/david/.myScripts/focusOrLaunch zen-beta zen-beta"; }
  Mod+4                      hotkey-overlay-title="Open spotify.com"             { spawn-sh "xdg-open https://www.spotify.com"; }
  Mod+Shift+4                hotkey-overlay-title="Restart spotifyd"             { spawn-sh "systemctl --user restart spotifyd"; }
  Mod+5                      hotkey-overlay-title="Open (or focus) Zotero"       { spawn-sh "/home/david/.myScripts/focusOrLaunch Zotero zotero"; }
  Mod+6                      hotkey-overlay-title="Open (or focus) Obsidian"     { spawn-sh "/home/david/.myScripts/focusOrLaunch obsidian obsidian"; }
  Mod+7                      hotkey-overlay-title="Toggle Settings"              { spawn-sh "noctalia-shell ipc call settings toggle"; }

  // screenshots
  Print        { screenshot; }
  Shift+Print  { screenshot-screen; }
  Alt+Print    { screenshot-window; }

  // change keyboard layout
  Mod+F1       { switch-layout "next"; }

  // send clipboard to Dropbox
  // Mod+Shift+Alt+Ctrl+C       hotkey-overlay-title="Fetch clipboard from Dropbox"            { spawn-sh "/home/david/.myScripts/db-fetch && notify-send 'db-fetch success'"; }
  // Mod+Shift+Alt+Ctrl+V       hotkey-overlay-title="Send clipboard to Dropbox"               { spawn-sh "/home/david/.myScripts/db-send && notify-send 'db-send: success'"; }
  Mod+Shift+Alt+Ctrl+B       hotkey-overlay-title="Send clipboard to Dropbox (overwrite)"   { spawn-sh "/home/david/.myScripts/db-send --clear && notify-send 'db-send -c: success'"; }

  // volume keys mappings for PipeWire & WirePlumber.
  XF86AudioRaiseVolume       allow-when-locked=true      { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.1+ --limit=1 && canberra-gtk-play -i audio-volume-change"; }
  XF86AudioLowerVolume       allow-when-locked=true      { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.1- && canberra-gtk-play -i audio-volume-change"; }
  XF86AudioMute              allow-when-locked=true      { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle && canberra-gtk-play -i audio-volume-change"; }
  XF86AudioMicMute           allow-when-locked=true      { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle"; }
  XF86AudioPlay              allow-when-locked=true      { spawn-sh "playerctl play-pause"; }
  XF86AudioPrev              allow-when-locked=true      { spawn-sh "playerctl previous"; }
  XF86AudioNext              allow-when-locked=true      { spawn-sh "playerctl next"; }

  // brightness key mappings for brightnessctl.
  XF86MonBrightnessUp        allow-when-locked=true      { spawn-sh "brightnessctl --class=backlight set +10%"; }
  XF86MonBrightnessDown      allow-when-locked=true      { spawn-sh "brightnessctl --class=backlight set 10%-"; }

  // closing windows and overview
  Mod+A repeat=false   { toggle-overview; }
  Mod+Q repeat=false   { close-window; }

  // focus (Mod+Direction) and move (Mod+Ctrl+Direction) windows accross workspaces
  Mod+Left             { focus-column-left; }
  Mod+Down             { focus-window-or-workspace-down; }
  Mod+Up               { focus-window-or-workspace-up; }
  Mod+Right            { focus-column-right; }
  Mod+H                { focus-column-left; }
  Mod+J                { focus-window-or-workspace-down; }
  Mod+K                { focus-window-or-workspace-up; }
  Mod+L                { focus-column-right; }
  Mod+Ctrl+Left        { move-column-left; }
  Mod+Ctrl+Down        { move-window-down; }
  Mod+Ctrl+Up          { move-window-up; }
  Mod+Ctrl+Right       { move-column-right; }
  Mod+Ctrl+H           { move-column-left; }
  Mod+Ctrl+J           { move-window-down-or-to-workspace-down; }
  Mod+Ctrl+K           { move-window-up-or-to-workspace-up; }
  Mod+Ctrl+L           { move-column-right; }
  Mod+Home             { focus-column-first; }
  Mod+End              { focus-column-last; }
  Mod+Ctrl+Home        { move-column-to-first; }
  Mod+Ctrl+End         { move-column-to-last; }

  // focus (Mod+Shift+Direction) and move (Mod+Shift+Ctrl+Direction) windows accross monitors
  Mod+Shift+Left       { focus-monitor-left; }
  Mod+Shift+Down       { focus-monitor-down; }
  Mod+Shift+Up         { focus-monitor-up; }
  Mod+Shift+Right      { focus-monitor-right; }
  Mod+Shift+H          { focus-monitor-left; }
  Mod+Shift+J          { focus-monitor-down; }
  Mod+Shift+K          { focus-monitor-up; }
  Mod+Shift+L          { focus-monitor-right; }
  Mod+Shift+Tab          { focus-monitor-next; }
  Mod+Shift+Ctrl+Left  { move-column-to-monitor-left; }
  Mod+Shift+Ctrl+Down  { move-column-to-monitor-down; }
  Mod+Shift+Ctrl+Up    { move-column-to-monitor-up; }
  Mod+Shift+Ctrl+Right { move-column-to-monitor-right; }
  Mod+Shift+Ctrl+H     { move-column-to-monitor-left; }
  Mod+Shift+Ctrl+J     { move-column-to-monitor-down; }
  Mod+Shift+Ctrl+K     { move-column-to-monitor-up; }
  Mod+Shift+Ctrl+L     { move-column-to-monitor-right; }

  // stacking windows in a column
  Mod+BracketLeft      { consume-or-expel-window-left; }
  Mod+BracketRight     { consume-or-expel-window-right; }
  Mod+Comma            { consume-window-into-column; }
  Mod+Period           { expel-window-from-column; }

  // managing window sizes
  Mod+R                 { switch-preset-column-width-back; }
  Mod+E                 { switch-preset-column-width; }
  Mod+Shift+R           { switch-preset-window-height; }
  Mod+Shift+E           { switch-preset-window-height-back; }
  Mod+Shift+F           { reset-window-height; }
  Alt+Ctrl+F            { maximize-window-to-edges; }
  Mod+F                 { maximize-column; }
  Mod+Ctrl+F            { fullscreen-window; }
  Alt+F                 { expand-column-to-available-width; }
  Mod+Minus             { set-column-width "-10%"; }
  Mod+Equal             { set-column-width "+10%"; }
  Mod+Shift+Minus       { set-window-height "-10%"; }
  Mod+Shift+Equal       { set-window-height "+10%"; }

  // misc - centering windows, floating windows, and tabbed column display
  Mod+I                { center-column; }
  Alt+I                { center-window; }
  Mod+T                { toggle-window-floating; }
  Mod+Ctrl+T           { switch-focus-between-floating-and-tiling; }
  Mod+W                { toggle-column-tabbed-display; }

  // move workspaces within a monitor
  Mod+Ctrl+Page_Down   { move-workspace-down; }
  Mod+Ctrl+Page_Up     { move-workspace-up; }
  Mod+Ctrl+U           { move-workspace-down; }
  Mod+Ctrl+I           { move-workspace-up; }

  // move workspaces across monitors
  Mod+Alt+H            { move-workspace-to-monitor-left; }
  Mod+Alt+J            { move-workspace-to-monitor-down; }
  Mod+Alt+K            { move-workspace-to-monitor-up; }
  Mod+Alt+L            { move-workspace-to-monitor-right; }

  // scroll wheel stuff - focus
  Mod+WheelScrollDown  { focus-column-right; }
  Mod+WheelScrollUp    { focus-column-left; }
  Alt+WheelScrollDown  cooldown-ms=150 { focus-workspace-down; }
  Alt+WheelScrollUp    cooldown-ms=150 { focus-workspace-up; }

  // Applications such as remote-desktop clients and software KVM switches may
  // request that niri stops processing the keyboard shortcuts defined here
  // so they may, for example, forward the key presses as-is to a remote machine.
  // It's a good idea to bind an escape hatch to toggle the inhibitor,
  // so a buggy application can't hold your session hostage.
  //
  // The allow-inhibiting=false property can be applied to other binds as well,
  // which ensures niri always processes them, even when an inhibitor is active.
  Mod+Delete allow-inhibiting=false { toggle-keyboard-shortcuts-inhibit; }
}

// alt+tab stuff
recent-windows {
    debounce-ms 750
    open-delay-ms 150

    highlight {
        active-color "#7aa2f7"
        urgent-color "#ff9999ff"
        padding 30
        corner-radius 12
    }

    previews {
        max-height 480
        max-scale 0.5
    }

    binds {
        Mod+Tab         { next-window; }
        Mod+Shift+Tab   { previous-window; }
        Mod+grave       { next-window     filter="app-id"; }
        Mod+Shift+grave { previous-window filter="app-id"; }
    }
}

hotkey-overlay {
    skip-at-startup
    hide-not-bound
}
